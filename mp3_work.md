# MP3 工作总结

## 目的

本文总结了当前这轮音频迭代中已完成的主要工作：

- 分步骤构建了什么
- 遇到了哪些问题
- 如何诊断这些问题
- 采用了哪些解决方案
- 目前还遗留了哪些未完成项

本文旨在保留当前 `WS2812 + MAX98357A + SD Audio` 主线实现背后的历史过程，方便后续会话无需从聊天记录中重建整个过程，就能继续推进工作。

## 起点

在本轮迭代开始时：

- 显示和触摸已经在重构后的 `clock_codex` 代码库中跑通
- 与语音相关的工作仍保留在仓库中，但还不够稳定，不能继续留在主线流程里
- 当前活动目标已从语音转向：
  - `WS2812` 灯光控制
  - 通过 `MAX98357A` 进行本地音频播放
  - 最终实现从 SD 卡播放 `WAV` 和 `MP3`

用户明确决定暂停语音相关工作，优先聚焦音乐播放。

## 阶段 1：WS2812 点亮与联调

### 目标

增加一条干净的 LED 测试路径，支持可选灯效与颜色。

### 已实现内容

- 确认 WS2812 数据引脚为 `GPIO46`
- 在 `components/bsp/include/board_config.h` 中新增板级 LED 配置
- 在 `components/ws2812` 中新增专用底层驱动
- 在 `components/app_services/app_led_service.c` 中新增服务层
- 在 `components/app_ui/app_ui.c` 中新增 UI 控件

### 新增的用户可见功能

- 灯效选择
- 颜色选择
- `Off`
- `Solid`
- `Blink`
- `Breathe`
- `Rainbow`

### 结果

`WS2812` 测试通过，并成为主线 LVGL 控制界面的一部分。

## 阶段 2：音频方案讨论

### 讨论过的选项

音乐播放路径在真正实现前做过讨论。主要候选方案有：

1. SD 卡本地播放
2. 手机蓝牙音频
3. Wi-Fi 流媒体播放

### 决策

项目最终转向 `SD -> 本地解码 -> I2S -> MAX98357A` 这条路线。

### 原因

- `ESP32-S3` 不支持经典蓝牙 `A2DP sink`，因此并不适合作为普通蓝牙音箱方案
- Wi-Fi 流媒体虽然被认为可行，但不适合作为首个里程碑
- SD 本地播放是最可控、最适合做 bring-up 的本地路径

## 阶段 3：MAX98357A 音调播放基线

### 已确认的硬件映射

用户确认了功放接线：

- `LRC` -> `GPIO6`
- `BCLK` -> `GPIO5`
- `DIN` -> `GPIO4`
- 喇叭接在 `OUT+/-`

### 已实现内容

- 在 `board_config.h` 中新增活动音频输出配置
- 创建 `components/audio_output`
- 在 `components/app_services/app_music_service.c` 中建立首版音调播放控制路径
- 在 LVGL UI 中新增 `Audio` 标签页

### 第一阶段功能目标

在真正接入文件播放之前，项目先使用简单的音调发生器作为音频输出基线：

- `A4`
- `C5`
- `E5`

用来确认以下内容：

- I2S TX 配置
- MAX98357A 输出链路
- 喇叭出声
- UI 播放控制

## 阶段 4：首次音频故障与稳定化

### 问题 1：音调只播放了短促一声，随后设备重启

### 观察到的现象

- 点击 `Play Tone` 后只发出一声很短的提示音
- 屏幕变白
- 开发板重启

### 初步识别出的风险点

当时同时识别出几条高风险路径：

- `ESP_ERROR_CHECK` 可能会把瞬时音频故障直接升级成整机复位
- I2S slot format 可能不适合这个功放
- DMA/缓冲区尺寸可能设置得过于激进
- 音频启动行为本身可能不稳定
- 也考虑过 brownout 或供电不稳的可能性

### 第一轮修复

- 将 I2S slot format 从先前设置改为 `Philips`
- 降低输出幅度
- 增大缓冲区大小
- 增大音乐任务栈
- 用错误处理和停止逻辑替代硬失败式的 `ESP_ERROR_CHECK`

### 结果

系统变得更稳定了，但根因仍未被完全证明。

## 阶段 5：增加诊断，而不是继续猜

### 原因

用户准确指出，前一轮稳定化虽然降低了风险，但并没有真正解释清楚确切根因。

### 新增诊断

- 启动时记录 reset reason
- 跨重启保留最近音频阶段标记
- 屏幕上显示音频诊断信息
- 更详细的状态输出

### 目标

区分以下情况：

- panic
- brownout
- watchdog
- I2S 写入失败
- 启动失败
- stop 路径清理行为

### 结果

问题从“模糊的重启”被收敛成了更精确的症状：

- 第一段音频突发能够听到
- 持续播放在 I2S 写入阶段失败
- 观察到了 `ESP_ERR_TIMEOUT`

这是一个关键进展，因为它说明问题不是“完全没声音”，而是“声音启动了，但随后卡死”。

## 阶段 6：修复 I2S 持续播放

### 问题 2：只有第一小段音频被播放

### 观察到的现象

- 能听到很短的音调
- 播放马上停止
- 状态指向 `ESP_ERR_TIMEOUT`
- 日志显示第一次写入只被部分接受

### 诊断

这表明以下几者之间存在不匹配：

- 应用层写入块大小
- DMA 缓冲
- 音频预装载行为
- 写入时序

### 采取的修复

- 调整应用层写入块大小
- 明确调优 DMA 描述符和 frame 参数
- 在正常播放开始前加入 preload 逻辑
- 后续又调整 preload 行为，以避免启动输出被破坏

### 结果

音调播放从“只有短促一声”变成了连续播放。

## 阶段 7：修复启动噪声

### 问题 3：播放启动时伴随可闻噪声

### 观察到的现象

- 在持续播放已经可行之后，点击播放时会先出现一段噪声
- 这段噪声之后，播放恢复正常

### 第一次尝试

曾加入淡入（fade-in）来减轻启动伪影。

### 用户反馈

用户准确指出，这种做法是在掩盖症状，而不是证明软件行为本身是正确的。

### 真正诊断

preload 逻辑为了填满 DMA 缓冲区，重复复用了同一段音频开头数据。
这会导致波形起始位置在同一相位被多次重新开始，听起来就像启动噪声。

### 真正修复

- 将 preload 从“重复音频数据”改为“预填静音”
- 然后在缓冲区预热完成后，再进入正常连续播放

### 结果

启动噪声问题被消除。

这是本轮迭代中的一个重要经验：

- 这个伪影是由软件缓冲区预热行为引起的
- 并不只是 MAX98357A 本身的问题
- 也不是简单加个外观层面的 fade-in 就能正确解决

## 阶段 8：SD 卡播放骨架

### 目标

从音调播放过渡到真实音频文件播放。

### 第一步

实现 `SD -> WAV -> I2S`。

### 新增组件

- `app_music_wav.c`
- SD 挂载逻辑
- 固定 WAV 路径处理
- UI 中 `Tone` 与 `SD Audio` 的源切换

### 初始错误

一开始错误地假设 SD 与 LCD/触摸共用同一条 SPI 总线。

### 问题 4：SD 挂载失败

### 观察到的现象

- 出现 `SD mount failed`

### 诊断

随后用户提供了真实的 SD 接线：

- `SCK` -> `GPIO18`
- `MISO` -> `GPIO19`
- `MOSI` -> `GPIO21`
- `CS` -> `GPIO38`

因此，先前关于共享总线的假设并不适用于这块板子。

### 修复

更新板级配置，改用独立的 SD SPI 引脚映射。

### 结果

`SD WAV` 播放开始正常工作。

## 阶段 9：SD 文件选择与测试资源处理

### 初始行为

最早的文件播放路径假定固定路径为：

- `/sdcard/music.wav`

### 原因

这原本只是用于 bring-up 的一条验证路径，用于确认：

- SD 挂载
- 文件打开
- WAV 解析
- 音频输出

### 改进

之后更新了 UI 和服务逻辑，使 `SD Audio` 可以：

- 扫描 SD 根目录
- 列出可播放文件
- 选择 `.wav`
- 后续也可以选择 `.mp3`

### 额外工作

期间还曾加入一条临时回退路径：在需要时通过 Wi-Fi 下载默认 WAV 文件，但最终 SD 本地文件选择成为了更实用的主线方案。

## 阶段 10：MP3 支持

### 目标

支持大多数音乐文件实际采用的格式。

### 已实现内容

- 新增 `components/app_services/third_party/minimp3`
- 新增 `app_music_mp3.c`
- 在音频源逻辑中加入 MP3 处理
- 在 `SD Audio` 下加入文件类型选择
- 在 `audio_output` 中启用运行时采样率重配置

### 当前支持的播放源路径

- 生成音调
- SD 上的 PCM WAV
- SD 上的 MP3

## 阶段 11：首轮 MP3 故障

### 问题 5：`TEST2.MP3` 报告 `MP3 open failed`

### 观察到的日志

日志中出现过以下情况：

- `unsupported mp3 stream`
- `hz=0`
- `channels=0`
- `prime returned no audio`

### 诊断

最初的 MP3 打开流程默认第一次解码尝试就应立即返回有效音频元数据。
但对于某些文件，尤其是 `TEST2.MP3`，解码器在第一步并不会给出可用的预热帧（priming frame）。

### 修复尝试

- 改进首帧探测
- 增加更明确的细节字符串
- 不在首次真正 prime 之前就过早拒绝文件

### 当前状态

`TEST2.MP3` 在当前轻量级解码路径下仍然会失败。

这目前属于一个已知限制，而不是隐藏 bug。

## 阶段 12：`TEST3.MP3` 能打开，但随后崩溃

### 问题 6：`TEST3.MP3` 打开后短暂播放，随后 panic 崩溃

### 观察到的日志

关键日志如下：

- MP3 成功打开，参数为 `48000 Hz, 2 ch`
- 出现大量 `i2s write ok` 日志
- 随后出现 `Guru Meditation Error`
- `InstrFetchProhibited`

### 诊断方向

这说明：

- SD 路径是通的
- 该文件的 MP3 打开流程是通的
- 音频输出采样率切换是通的
- 故障发生在持续解码/播放过程中，而不是文件打开阶段

### 当时考虑的高风险原因

- 任务栈耗尽
- 解码器缓冲区使用不当
- 分片帧处理错误
- 返回的解码指针被无效复用

### 已采取的修复

- 先将音乐任务栈增至 `16 KB`，之后又增至 `24 KB`
- 增加栈水位日志
- 修改 MP3 处理逻辑，不再依赖半帧或碎片化的解码行为
- 改为缓存式完整帧解码模型：
  - 先将一帧解码到自有存储中
  - 保持这份数据缓存
  - 播放时从缓存帧提供数据
  - 仅在缓存帧被消费完后再进行下一次填充

### 结果

`TEST3.MP3` 变得稳定且可播放。

这是整个迭代中最重要的结构性修复之一。

## 阶段 13：状态显示与 UI 调试改进

### 为什么需要

在音频和 MP3 bring-up 期间，UI 曾让大量调试时间白白流失，因为它会：

- 让标签和控件相互覆盖
- 隐藏状态文本
- 过快覆盖掉有价值的错误状态

### 已增加的改进

- 将状态区域下移
- 将音频页面部分区域改为可滚动
- 让状态文本保留足够长时间，便于阅读
- 将源层更详细的错误文本暴露给 UI

### 结果

设备在调试时变得更容易使用，不再完全依赖串口输出。

## 阶段 14：向更清晰的音频边界进行内部重构

### 原因

随着音频路径从 `Tone` 扩展到 `WAV` 再到 `MP3`，代码需要更明确的所有权边界。

### 架构方向

这轮工作逐渐对齐为以下几类职责：

- source 逻辑
- output 逻辑
- UI 逻辑

而不是把所有内容都塞进一条服务路径中。

### 当前代码中的方向

- `audio_output` 负责 I2S 播放输出
- `app_music_wav` 负责 WAV 解析
- `app_music_mp3` 负责 MP3 解码
- `app_music_source` 提供更清晰的音源抽象
- `app_music_service` 负责播放控制和面向 UI 的状态

### 为什么重要

这让后续工作，尤其是 USB 音频准备，相比直接把 USB 行为硬塞到一个庞杂的 `SD Audio` 路径中，要清晰得多。

## 关键问题与解决方案总结

### 1. 音调播放时重启

问题：

- 播放只响一声后就重启

解决方案：

- 去掉硬失败行为
- 调整 I2S 格式和缓冲策略
- 增加诊断

### 2. 只能听到短促一声，无法连续播放

问题：

- 第一块数据播放后出现 `ESP_ERR_TIMEOUT`

解决方案：

- 调优 DMA 和写入块大小
- 改善 preload 和输出启动行为

### 3. 正常播放前先有启动噪声

问题：

- 播放启动时先有一段噪声爆发

解决方案：

- 找到“重复音频 preload”这一根因
- 将 preload 改为静音

### 4. SD 挂载失败

问题：

- SPI 总线假设错误

解决方案：

- 修正 SD 总线接线为独立引脚

### 5. 某些 MP3 文件在首帧 prime 阶段失败

问题：

- `hz=0`、`channels=0`、`prime returned no audio`

解决方案：

- 改进探测与错误细节输出
- 接受部分文件仍超出当前轻量解码器容忍范围这一现实

### 6. MP3 打开后播放崩溃

问题：

- `TEST3.MP3` 打开后又因 `InstrFetchProhibited` 崩溃

解决方案：

- 增大栈
- 增加栈诊断
- 以缓存式完整帧处理替代碎片化解码使用方式

## 当前稳定结果

当前主线已经支持：

- `WS2812` 灯光控制
- `MAX98357A` 音频输出
- 稳定的音调播放
- 稳定的 SD WAV 播放
- 至少一个已知良好的 MP3 文件 `TEST3.MP3` 的稳定播放
- 通过 UI 控制：
  - 音调与 SD 音频源切换
  - 文件选择
  - 播放 / 停止流程

## 当前已知限制

当前 MP3 方面剩余的主要限制是：

- `TEST2.MP3` 仍会在当前基于 `minimp3` 的路径中，在解码器 priming 阶段失败

这意味着：

- 当前 MP3 实现已经可用
- 但对任意 MP3 编码格式仍不够健壮

## 与本轮迭代最相关的文件

- `components/bsp/include/board_config.h`
- `components/ws2812/ws2812_driver.c`
- `components/app_services/app_led_service.c`
- `components/audio_output/audio_output.c`
- `components/app_services/app_music_service.c`
- `components/app_services/app_music_source.c`
- `components/app_services/app_music_wav.c`
- `components/app_services/app_music_mp3.c`
- `components/app_ui/app_ui.c`
- `docs/current-status.md`
- `docs/hardware-wiring.md`

## 建议的后续衔接点

阅读完本文后，最合理的下一步是：

1. 继续把 `Tone`、`WAV` 和 `TEST3.MP3` 作为回归音源
2. 继续拆分音源与输出职责
3. 决定下一步是：
   - 继续优化 `minimp3`
   - 还是在需要更广泛 MP3 兼容性时将其替换
4. 仅在当前本地播放路径稳定之后，再开始面向 USB 音频的重构
